[
  {
    "id": "breakeven_single_sided",
    "name": "Single-Sided Breakeven Reallocator",
    "active": true,
    "description": "When the price stays out-of-range long enough, open the closest possible single-sided range and choose the opposite boundary so that hitting it yields price-only breakeven (>= baseline * (1+buffer)). If not achievable at minimum width, return wait.",
    "params": {
      "minimum_minutes_out_of_range": 1,
      "min_ticks_from_price_on_near_side": 1,
      "min_width_multiples_of_spacing": 1,
      "breakeven_buffer_pct": 0.001,
      "max_opposite_side_expansions": 10
    }
  }
]